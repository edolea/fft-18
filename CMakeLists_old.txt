cmake_minimum_required(VERSION 3.10)

# Project name
project(FFTProject)

# C++ standard
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Add the executable
add_executable(FFTMain
        FFT/src/fft_transform.cpp
        FFT/src/dwt_transform.cpp
        FFT/src/signal_utils.cpp
        FFT/src/main.cpp
        FFT/src/signal_visualizer.cpp
        FFT/src/optimized_fft.cpp
        FFT/src/signal_generator.cpp
)

# Add the executable
add_executable(FFTVisualizationDemo
        FFT/src/fft_transform.cpp
        FFT/src/dwt_transform.cpp
        FFT/src/signal_utils.cpp
        FFT/src/signal_visualizer.cpp
        FFT/src/optimized_fft.cpp
        FFT/src/signal_generator.cpp
        FFT/src/visualization_demo.cpp
)

# add executable
add_executable(fft
        src/main.cpp
        src/fourier_transform.cpp
)



# Include directories
include_directories(
        FFT/include
        include
)
# include_directories(${MPI_INCLUDE_PATH})

# Link libraries
# find_package(Threads REQUIRED)
# find_package(OpenMP REQUIRED)
# find_package(MPI REQUIRED)

# target_link_libraries(FFTProject ${MPI_LIBRARIES} OpenMP::OpenMP_CXX)
# target_link_libraries(FFTProject Threads::Threads)

# Set compiler flags
# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -fopenmp")